#!/bin/sh

layout=$(mmsg get keyboardlayout 2>/dev/null | sed -n 's/.*"layout":"[^(]*(\([^)]*\)).*/\1/p')
[ -z "$layout" ] && layout=$(cat "$HOME/.cache/layout" 2>/dev/null || echo "?")

printf '{ "text": "  %s", "class": "block" }\n' "$layout"
